<form method="post" action="{{ path('admin_delete', {'id': admin.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer ce compte?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ admin.id) }}">
    <button class="button is-small is-danger has-font-poppins">Supprimer</button>
</form>
