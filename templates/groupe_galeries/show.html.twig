{% extends 'base.html.twig' %}

{% block title %}GroupeGaleries{% endblock %}

{% block body %}
    <h1>GroupeGaleries</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ groupe_galery.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('groupe_galeries_index') }}">back to list</a>

    <a href="{{ path('groupe_galeries_edit', {'id': groupe_galery.id}) }}">edit</a>

    {{ include('groupe_galeries/_delete_form.html.twig') }}
{% endblock %}
