{% extends 'base.html.twig' %}

{% block title %}Modifier l'Oeuvre{% endblock %}

{% block body %}
<div class="container is-flex is-flex-direction-column is-justify-content-center is-align-items-center pt-6 mt-6">
    <h1>Modifier l'oeuvre</h1>

    <div class="column is-full is-flex is-justify-content-start">
        <a class="button is-small is-dark" href="{{ path('oeuvre_index') }}">Retour à la liste</a>
    </div>

    {{ include('oeuvre/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('oeuvre/_delete_form.html.twig') }}
</div>
{% endblock %}
