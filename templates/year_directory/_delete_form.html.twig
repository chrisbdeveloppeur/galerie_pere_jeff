<form method="post" action="{{ path('year_directory_delete', {'id': year_directory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ year_directory.id) }}">
    <button class="btn">Supprimer</button>
</form>
