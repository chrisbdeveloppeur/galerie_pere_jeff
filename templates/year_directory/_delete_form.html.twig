<form method="post" action="{{ path('year_directory_delete', {'id': year_directory.id}) }}" onsubmit="return confirm('êtes-vous sur de vouloir supprimer cette galerie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ year_directory.id) }}">
    <button class="button is-small is-danger has-font-poppins">Supprimer</button>
</form>
