{% extends 'base.html.twig' %}

{% block title %}Edit YearDirectory{% endblock %}

{% block body %}
    <h1>Edit YearDirectory</h1>

    {{ include('year_directory/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('year_directory_index') }}">back to list</a>

    {{ include('year_directory/_delete_form.html.twig') }}
{% endblock %}
