{% extends 'base.html.twig' %}

{% block title %}YearDirectory{% endblock %}

{% block body %}
    <h1>YearDirectory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ year_directory.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ year_directory.title }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ year_directory.year }}</td>
            </tr>
            <tr>
                <th>FileName</th>
                <td>{{ year_directory.fileName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('year_directory_index') }}">back to list</a>

    <a href="{{ path('year_directory_edit', {'id': year_directory.id}) }}">edit</a>

    {{ include('year_directory/_delete_form.html.twig') }}
{% endblock %}
